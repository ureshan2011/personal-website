# The book PDF is gated content — it lives in Firebase Storage behind
# storage.rules, not in this public repo. See PLATFORM_SETUP.md.
assets/files/The-Collaboration-Reflex-Book.pdf

.DS_Store

# Local-only tooling for scripts/upload-book.js (firebase-tools, firebase-admin)
node_modules/
package.json
package-lock.json
